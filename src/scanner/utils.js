const screenshotDebugStoragePath = `${process.cwd()}/src/debug-screenshots`;
const resultsPath = `${process.cwd()}/src/results`;

const utils = {
  screenshotDebugStoragePath,
  resultsPath,
};

module.exports = utils;
